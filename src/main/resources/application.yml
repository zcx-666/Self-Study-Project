server:
  port: 1234

spring:
  datasource:
    url: jdbc:mysql://localhost/study
    username: root
    password: root

debug:
  false

swagger:
  enabled: true
  title: Self-Study-Api
  exclude-path:
    - /error
    - /ops/**

mybatis:
  mapper-locations:
    classpath:com.example.study.mapper/*.xml

#  configuration:
#     log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 设置打印sql语句

